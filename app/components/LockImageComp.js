import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Icon from ''

export default function LockImageComp() {
  return (
    <View>
      <Text>LockImageComp</Text>
    </View>
  )
}

const styles = StyleSheet.create({})